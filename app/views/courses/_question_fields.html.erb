<p>
	<%= f.label :content, "Question" %><br />
	<%= f.text_area :cotnent, :rows => 3 %><br />
	<%= f.check_box :_destroy %>
	<%= f.label :_destroy, "Remove Question" %>
</p>
<%= f.fields_for :answers do |builder| %>
	<%= render 'choice_fields', :f => builder %>
<% end %>