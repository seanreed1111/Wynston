<%= form_for(@course_module) do |f| %>
  <% if @course_module.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@course_module.errors.count, "error") %> prohibited this course_module from being saved:</h2>

      <ul>
      <% @course_module.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "Lesson Title" %><br />
    <%= f.text_field :title %>
  </div>

<!--   associates modules edits with a user -->
  <div class="field">
    <%= f.hidden_field :user_id, :value => current_user.id %><br />
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
<br><br>
