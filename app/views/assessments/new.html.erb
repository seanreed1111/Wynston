<h1>Assessment for 
  <% if @assessment.course_module_id %>
    <%= @assessment.course_module_id %>
  <% else %>
    "Current Module has no name."
  <% end %>
</h1>

<%= render 'form' %>

<%= link_to 'Back', users_path %>
